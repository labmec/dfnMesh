########### next target ###############


# file(GLOB ${util_headers} *.h *.hpp *.tpp)
# file(GLOB ${util_sources} *.cpp)

# list(APPEND ${headers} ${util_headers})
# list(APPEND ${sources} ${util_sources})

# include_directories(.)

# target_include_directories(dfnTest PUBLIC 
#                            $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
#                         #    $<INSTALL_INTERFACE:${NEOPZ_INCLUDE}/External/FAD>
#                           )