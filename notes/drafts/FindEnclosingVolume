// REAL dot = 0.0;
// if(dot + fTolerance >= 1.) {foundPerfectCandidate = true; break;}
// if(foundPerfectCandidate) break;
// // If neighbour has an enclosing volume, list it as a candidate volume
// if(ineig.Element()->MaterialId() >= surfaceMaterial){
// 	for(auto fracmesh = fFractures.begin(); fracmesh != fFractures.end(); fracmesh++){
// 		DFNFace *dfnNeig = fracmesh->Face(ineig.Element()->Index());
// 		if(!dfnNeig) continue;
// 		if(dfnNeig->EnclosingVolume() == -1) break;
// 		if(candidates_set.find(dfnNeig->EnclosingVolume()) == candidates_set.end()) break;
// 		candidates_set.insert(dfnNeig->EnclosingVolume());

// 	}
// }